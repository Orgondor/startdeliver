## Startdeliver backend test

To run, follow these steps:
- Clone repository
- Run: `npm install` in the repository root folder
- Create a copy of .env.default called .env (`cp .env.default .env`) and replace the api keys in the file
- Run: `npm run build`
- Run: `npm run test`